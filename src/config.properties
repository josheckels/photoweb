
PhotosDirectory=/Volumes/Crucial X8/www/jeckelsImages/photoGallery
ResizedDirectory=/Volumes/Crucial X8/www/jeckelsImages/resized

ResizedURL=file:///Volumes/Crucial%20X8/www/jeckelsImages/resized/
PhotosURL=file:////Volumes/Crucial%20X8/www/jeckelsImages/photoGallery/

# AWS S3 upload settings (optional). If missing, S3 uploads are disabled.
S3Region=us-west-2
S3OriginalsBucket=photo.jeckels.com
S3ResizedBucket=resized.jeckels.com
#S3KeyPrefix=photoGallery/

# Which ~/.aws/credentials profile to sign with. Set this on any machine that also has AWS credentials for
# something else: without it the SDK's default chain prefers environment variables over the profile file, so
# whichever account the shell (or the IDE's inherited environment) happens to be holding would win. Naming a
# profile here uses that profile and consults nothing else. The name is not a secret; the key stays in
# ~/.aws/credentials. Leave unset to use the default chain.
#S3Profile=photoweb

ExportedPhotosDirectory=/Users/jeckels/Pictures/Exported